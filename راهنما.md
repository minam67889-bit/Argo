# راهنمای فارسی Argo

## چی هست؟

یه بازنویسی کامل پروژه‌ی قبلی (llm-uncensored-toolkit). همون کار رو می‌کنه ولی:

- **UI حرفه‌ای** (سایدبار، چند چت، تم تیره/روشن، markdown، استریم)
- **ایجنت واقعی** با parser قوی که با همه‌ی مدل‌ها کار می‌کنه
- **6 ابزار** تست‌شده: bash, read_file, write_file, edit_file, list_dir, search_files
- **حلقه‌ی ReAct** با تشخیص حلقه، مدیریت context، محدودیت زمانی
- **بدون Colab** — روی هر سرور/لپ‌تاپ اجرا میشه
- **بدون کامپایل** — فقط `pip install` و `python -m app.main`
- **چت و CLI** هر دو

## شروع سریع

### 1) نصب

```bash
git clone <repo> argo
cd argo
pip install -r requirements.txt
```

### 2) تنظیم کلید API

```bash
export LLM_API_KEY="sk-or-..."            # از openrouter.ai/keys (رایگان)
export LLM_BASE_URL="https://openrouter.ai/api/v1"
export LLM_MODEL="qwen/qwen3-coder:free"   # رایگان
```

یا هر OpenAI-compatible دیگه:

```bash
# Ollama
export LLM_BASE_URL="http://localhost:11434/v1"
export LLM_API_KEY="ollama"
export LLM_MODEL="qwen2.5-coder:14b"

# llama.cpp server
export LLM_BASE_URL="http://localhost:8080/v1"
export LLM_API_KEY="sk-no-key-required"
export LLM_MODEL="local-model"

# vLLM
export LLM_BASE_URL="http://localhost:8000/v1"
export LLM_API_KEY="token-abc123"
```

### 3) اجرا

```bash
# وب — http://localhost:8000
python -m app.main

# CLI
python -m cli.agent "این پروژه رو تحلیل کن"
python -m cli.agent --workdir /path/to/project --auto
python -m cli.agent
```

### 4) یا با اسکریپت

```bash
./scripts/dev.sh install
./scripts/dev.sh run
./scripts/dev.sh cli "task"
./scripts/dev.sh test
```

## مدل‌های پیشنهادی (رایگان)

| مدل | کاربرد | تنظیم |
|---|---|---|
| `qwen/qwen3-coder:free` | کد + ایجنت | OpenRouter |
| `z-ai/glm-4.5-air:free` | چت عمومی | OpenRouter |
| `moonshotai/kimi-k2.7-code` | بهترین ایجنت (پولی ارزون) | OpenRouter |

## ساختار پروژه

```
argo/
├── app/
│   ├── main.py             # FastAPI entrypoint
│   ├── api/                # REST endpoints
│   │   ├── routes.py
│   │   └── schemas.py
│   ├── agent/              # حلقه ایجنت
│   │   ├── loop.py
│   │   ├── parser.py
│   │   └── prompts.py
│   ├── tools/              # 6 ابزار
│   │   ├── base.py
│   │   ├── bash.py
│   │   ├── read_file.py
│   │   ├── write_file.py
│   │   ├── edit_file.py
│   │   ├── list_dir.py
│   │   └── search_files.py
│   ├── core/               # تنظیمات + LLM client + storage
│   │   ├── config.py
│   │   ├── llm.py
│   │   ├── storage.py
│   │   └── logging.py
│   ├── static/             # CSS + JS
│   │   ├── css/app.css
│   │   └── js/app.js
│   └── templates/
│       └── index.html
├── cli/                    # ترمینال
│   └── agent.py
├── tests/                  # تست
├── docs/                   # مستندات
├── scripts/dev.sh
├── requirements.txt
└── README.md
```

## حالت‌ها

### چت (`chat`)
- فقط متن، بدون ابزار
- streaming سریع
- مناسب گفتگوی ساده

### ایجنت (`agent`)
- 6 ابزار در دسترس
- حلقه ReAct (تفکر → ابزار → مشاهده → ...)
- تشخیص حلقه (اگه 3 بار یه کار تکرار بشه متوقف میشه)
- مدیریت context (پنجره لغزان)
- حداکثر گام قابل تنظیم

## تفاوت با پروژه قبلی

| ویژگی | قبلی | Argo |
|---|---|---|
| UI | HTML inline کوتاه | مدرن، سایدبار، چند چت |
| ابزار | 5 تا، بعضی‌ها خراب | 6 تا، همه تست‌شده |
| parser | regex شکننده | 4 فرمت + native OpenAI |
| حلقه ایجنت | ابتدایی | قوی + loop detection + context mgmt |
| streaming | نداره/خراب | SSE کامل (text, reasoning, tool events) |
| storage | localStorage مرورگر | SQLite |
| تنظیمات | فقط env | UI + env + per-chat |
| نیاز به Colab | بله | نه، هر جا |
| CLI | فایل ساده | REPL با /reset, /mode |
| تست | نداره | unit test برای parser + tools |

## عیب‌یابی

### "LLM_API_KEY تنظیم نشده"
```bash
export LLM_API_KEY="sk-..."
```

### "Connection refused" / خطای شبکه
مطمئن شو `LLM_BASE_URL` درسته. برای OpenRouter: `https://openrouter.ai/api/v1`

### ایجنت مدام یه کار رو تکرار می‌کنه
تشخیص حلقه بعد از 3 بار فعال میشه. اگه می‌خوای بیشتر صبر کنه:
```bash
export AGENT_LOOP_THRESHOLD=5
```

### پاسخ‌ها خیلی کوتاه
`max_tokens` رو بیشتر کن:
```bash
export AGENT_MAX_TOKENS=16384
```

### پاسخ‌ها خیلی تصادفی
`temperature` رو کم کن:
```bash
export AGENT_TEMPERATURE=0.1
```

## لایسنس

MIT — استفاده‌ی تجاری آزاد.
